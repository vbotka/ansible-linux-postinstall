---

task_service_synopsis: |
  Manage services.

task_service_description: |
  Debug (4)
  ^^^^^^^^^

  TBD

  Sanity (23)
  ^^^^^^^^^^^

  TBD

  Automatic management of listed services (73)
  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

  When not empty (74) iterate ``my_services_varnames`` (84) (See default s/main/service.yml). Skip
  when ``run_task`` (93) is not *True*.

  Manual management of listed services (103)
  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

  When not empty (104) iterate ``lp_service`` in the included tasks (114). When *auto* (111) set the
  module (110) to ``ansible_service_mgr`` (OS native service manager found by the *setup*). TBC

  Flush handlers (116)
  ^^^^^^^^^^^^^^^^^^^^

  Automatic management of listed services notifies ``ansible_service_mgr``. See the tasks
  service-\*.yml how manual management options notify handlers.

task_service_lines: 2,4,23,73,103,116
...
