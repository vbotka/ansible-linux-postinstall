# {{ ansible_managed }}

# default
{% for conf in item.default|d([]) %}
{{ conf.key }} {{ conf.value|default('') }}
{% endfor %}

# user
{% for conf in item.conf %}
{{ conf.key }} {{ conf.value|default('') }}
{% endfor %}
